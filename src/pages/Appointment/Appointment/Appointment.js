import React from 'react';
import AppointmentBanner from '../AppointmentBanner/AppointmentBanner';

const Appointment = () => {
    return (
        <div>
            <h1>This is an appointment </h1>
            <AppointmentBanner></AppointmentBanner>
        </div>
    );
};

export default Appointment;